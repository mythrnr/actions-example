name: "composite-private"

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v2"

      - uses: "actions/checkout@v2"
        with:
          repository: "mythrnr/workspace"
          token: "${{ secrets.SECRET_TOKEN }}"
          path: "./.github/external-actions"

      - uses: "./.github/external-actions/.github/actions/private-example"
